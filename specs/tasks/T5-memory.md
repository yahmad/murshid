# T5 — Memory

**Status:** Planned · **Traces to:** SPEC v0.5 D21/D22, I22–I26, C4/C5/C8/C12.
`concept_memory` table (C5); BKT update (per-category priors, C12) from
implicit evidence: stage-1 application detections (activate the reserved
output field from T1) = pass; validated finding on taught concept = fail;
same-site same-session after help = hard (correct, no fade); dismissals =
no observation. Entry-rung selection wired into T4's ladder (C4 table);
p ≥ 0.95 ⇒ silence + announced fade; `murshid progress` open meter;
level-down on regression; staleness windows (C12); ≤ 2 scarcity-triggered
retrieval questions at session boundaries, judge-graded (C8).
Acceptance sketch: mastery rises from clean applications without any card
shown; fade is visible in the meter and stated once.
