# T4 — Card interaction

**Status:** Planned · **Traces to:** SPEC v0.5 D5/D17–D20, I15–I21, C3/C4/C6.
Full C3 response enum incl. `applied`/`escalated`; C4 rung ladder rendering
(R0–R3, directness knob offset; entry from memory once T5 lands, static
default until); worked-diff unfold (bottom-out = commented worked example,
reveal logged); card-anchored follow-up threads (turn cap 5, thread verbs
card-scoped, transcripts stored per C5); `// murshid:` channel (pack token,
pull-priced, suppression-reset, unresolved list in bookend); `murshid
review` solicited digest (top 3, at commit detection/on demand/bookend,
EFP-exempt logging); BYOK consent prompts (C6).
Acceptance sketch: thread cannot exceed cap; direct ask owns the slot;
review output passes the D18 parity test (goal + memory context present).
