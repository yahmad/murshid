# T3 — Session goals & struggle prompts

**Status:** Planned · **Traces to:** SPEC v0.5 D13–D16, I8–I14, C7/C12.
Infer-first goal banner (branch/issue/commits), editable goal file, `goal`
command; goal-relevance queue priority (C7); session-end bookend (one
screen: goal vs happened, queued, ignored); struggle signals — repeated
same-E-code failures (3×) AND time-in-red > self 75th pct (cold start
25 min) converge ⇒ one-line offer at idle, evidence shown, y/N, expiring;
help-flavored fresh comments prompt alone (on-hold TODOs excluded);
prompts share the push budget (borrow ≤ 1 token), category
"struggle-offer" feeds throttle.
Acceptance sketch: no goal entry ever blocks; prompt never fires while
typing; declines persist and suppress.
